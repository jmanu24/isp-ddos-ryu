# git+faucetsdn's fork, not PyPI's `ryu==4.30`/`ryu>=4.34` -- confirmed on
# TWO real runs (this lab's fresh orchestrator VM, Python 3.10, AND the
# existing Mininet-based Ubuntu test VM's own system-wide install,
# Python 3.8 -- both currently broken, so this predates and is unrelated
# to anything about the distributed-VM lab) that PyPI's osrg/ryu package
# (unmaintained since ~2021, whichever version) does
# `from eventlet.wsgi import ALREADY_HANDLED` unconditionally in
# ryu/app/wsgi.py, and no eventlet version still exports that name at
# module level (only a newer, incompatible WSGI_LOCAL.already_handled
# flag API). faucetsdn/ryu (the actively-maintained fork behind the
# Faucet SDN controller) already fixed exactly this:
# `_ALREADY_HANDLED = getattr(eventlet.wsgi, "ALREADY_HANDLED", None)`
# (confirmed via `gh search code "ALREADY_HANDLED" --repo faucetsdn/ryu`).
# No PyPI releases from this fork, only git -- pinned to master's own
# HEAD at the time of this fix (2022-06-09, "Add project status").
ryu @ git+https://github.com/faucetsdn/ryu.git@d6cda4f427ff8de82b94c58aa826824a106014c2
numpy>=1.26
Flask==3.0.3
Flask-SocketIO==5.3.6
python-socketio==5.11.2
eventlet==0.36.1
prometheus-client==0.20.0
